import { Meta } from "@storybook/react-vite";

import Loading from ".";

export default {
  title: "classic/atoms/Loading",
  component: Loading,
} as Meta;

export const Default = () => <Loading />;
